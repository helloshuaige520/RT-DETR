我的创新点对比其他人做过的优势在哪，对比其他人的算法，我的算法改进了哪些地方
1.把边缘当成“结构先验”，直接参与 RT-DETR 的特征建模过程

具体优势：
❌ 不是单纯“边缘增强”
✅ 是边缘 → 引导特征融合 / 注意力分配
与现有方法仅在骨干网络或预处理阶段引入边缘信息不同，本文将边缘先验作为一种结构约束，嵌入到 RT-DETR 的特征融合与编码过程中，使模型在端到端训练中显式关注小目标的几何边界。