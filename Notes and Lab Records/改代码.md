# 路线：

Day 1（**认世界**）：整体 + PyTorch 最小闭环

什么是深度学习（训练 ≠ 推理）

PyTorch 是干什么的

什么是 Tensor（张量）

什么是 forward（前向）

PyTorch → ONNX → TensorRT 在干什么

### ② Python 最低语法（1.5 h）

只看 / 只用这些：

- 变量
- if / for
- list / dict
- 函数（def）
- 看检测算法中代码

③ class / **init** / forward（2 h，重中之重）

 **init** 在搭结构，forward 在算数据

④PyTorch 训练 5 行

model = Model(cfg)
optimizer = torch.optim.Adam(model.parameters())
loss = criterion(pred, gt)
loss.backward()
optimizer.step()

###  Day 1 结束检查

- forward 是什么时候被调用的？
- loss.backward() 在干什么？
- shape 为什么重要？

Day 2（**懂检测**）：YOLO + DETR

能 3 分钟讲完 YOLO

知道为什么要 NMS

知道 anchor-free 是什么

## ② DETR / RT-DETR（3 h，拉开差距）

你要能**非常清楚地讲**：

- Transformer-based detector 是什么
- Query 是什么（核心）
- Set Prediction 是什么
- 为什么 DETR 不需要 NMS
- Hungarian Matching 是训练时干什么的

③ YOLO vs DETR（1 h）

| 项目     | YOLO     | DETR       |
| -------- | -------- | ---------- |
| 检测方式 | 密集预测 | Query      |
| NMS      | 需要     | 不需要     |
| 收敛     | 快       | 慢         |
| 小目标   | 好       | 差（原始） |

### ✅ Day 2 结束检查

你能**不结巴**地说清：

> “DETR 为什么不需要 NMS？”

# 🗓️ Day 3（**像工程师**）：部署 + 面试表达

## ⏰ 用时：5–7 小时

------

## ① PyTorch → ONNX → TensorRT（3 h）

你目标只有一个：

> **“我知道每一步在干什么”**

你要能说：

- PyTorch：训练
- ONNX：中间表示
- TensorRT：GPU 推理加速

不学 API，不写 C++。

------

## ② Debug 必会（1 h）

你要知道：

```
print(x.shape)
assert x.shape[1] == 256
```

是在干什么。

------

## ③ 面试标准回答准备（2–3 h）

你必须准备好的 5 句话：

1. 我 Python 能看懂和修改深度学习代码
2. 我主要做过 RT-DETR 小目标检测实验
3. YOLO 和 DETR 各有工程取舍
4. 我了解 ONNX / TensorRT 的部署流程
5. 我目前在补工程化能力

# 例程：

```python
class DetectHead(nn.Module):
    def __init__(self, num_classes):
        super().__init__()
        self.conv = nn.Conv2d(256, num_classes, 1)
        
def forward(self, x):
    return self.conv(x)
```

`.` = “这个东西里面的某个成员”

self.conv = nn.Conv2d(3, 16, 3)

在对象self里有个成员是conv

# 笔记：

## 为什么图像在计算机里长这样：

```
[batch, channel, height, width]
```

比如：
[1, 3, 640, 640]

1 张图

3 个通道（RGB）

640 × 640

## forward

> **forward = 数据从“输入”到“输出”的过程**

就像：

```
输入图像
→ backbone
→ neck
→ head
→ 输出预测
```

## 什么是 Tensor（张量）

你以后看到的所有数据，都是 **Tensor**

### 从简单开始

```
数字      → 0
向量      → [1, 2, 3]
矩阵      → [[1,2],[3,4]]
张量      → 更高维
```